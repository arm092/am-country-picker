export default {
    //Countries codes that will be excluded from list
    exclude: [],

    //Show flags with names of the countries
    flags: false,

    //Flags sprite image file
    flagsSprite: '../images/flags.webp',
}
